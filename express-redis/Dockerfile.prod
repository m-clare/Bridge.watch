#------------------>Production image
FROM node:lts-alpine
RUN apk add dumb-init
RUN apk add sudo
RUN apk add curl

WORKDIR /usr/src/app
COPY package*.json /usr/src/app/
RUN npm install 
CMD ["dumb-init", "node", "server.js"]